import Vue from 'vue';
import Vuex from 'vuex';

Vue.use(Vuex);

export const Store = {
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
};

export default new Vuex.Store(Store)
